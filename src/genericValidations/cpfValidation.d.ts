import RegexValidation from "../regexValidation";
export default class CpfValidation extends RegexValidation {
    constructor();
    private checkCpfLogic;
    validateUnmasked(input: string): boolean;
}
